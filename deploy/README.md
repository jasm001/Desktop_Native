# Deployment

Production and pilot deployment assets are added only after their corresponding
security and environment gates are approved.

`local-demo/manifests/` contains development-only artifact metadata used for VM
validation. It does not contain installers, credentials, production endpoints,
UEMS packages, signed policies, or a deployable corporate configuration.
